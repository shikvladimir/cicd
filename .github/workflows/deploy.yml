name: Deploy to Server

on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello, world!
          
      # - name: Запуск команд на сервере
      #   run: |
      #     ssh root@194.62.19.57 << 'EOF'
      #       cd /home/cicd
      #       git pull origin main
      #     EOF
